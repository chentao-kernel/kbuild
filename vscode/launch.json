{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Native Debugger for Linux",
            "type": "gdb",
            "request": "attach",
            "executable": "${workspaceRoot}/vmlinux",
            "gdbpath": "gdb",
            "target": ":1234",
            "remote": true,
            "cwd": "${workspaceRoot}"
        }
    ]
}
